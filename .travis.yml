dist: xenial

language: python

cache: pip

python:
    - "3.6"
    - "3.7"
    - "3.8"
    - "nightly"

matrix:
    allow_failures:
        - python: "nightly"

install:
  - pip install cookiecutter
  - pip install pipenv --upgrade-strategy=only-if-needed

script:
- bash ./test.sh